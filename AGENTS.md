# RF-3DGS 项目约定

## 时间类指标的报告契约
- 任何计时必须报全链:数据生成 + 训练 + 评估,分项 + 合计。
  只报训练时间视为不完整,退回。
- 必须声明:GPU 型号、是否独占。GPU 被其他任务占用时的计时一律作废
  (历史上有 34% / 41% / 99% 占用下的记录,不可用)。
- 渲染时间必须标分辨率和面数。300×200 单面的数不可外推:
  该尺度下 kernel launch 与 Python 编排支配总时间,不测光栅器。
  声称"X 比 Y 快"必须至少给两个分辨率。
- 一张完整球面谱 = 4 面 + 拼接。单面时间不得表述为"渲染一张谱"。
- 排除 warm-up 迭代;报 wall-clock 中位数,不报最好一次。

## 基线公平性
- 与发布实现对比时,必须同时给出两行:
  「as published」和「as published + 已知工程修正」。
  后者未测时,标 MISSING,不得省略该行。
- 加速比必须分解为「工程」与「方法」两部分。不可分解则不声称。
- 表格中缺失项写 MISSING 或 N/A,不得静默删行。

## Smoke 优先
任何新算法、新指标、新数据管线,必须先跑 smoke,通过后才申请
全量 GPU 时间。全量运行前必须贴出 smoke 的结果。

### smoke 必须保住的东西(只缩尺寸,不缩结构)
- 分层:视角抽样按朝向/位置分层,不得用 `[::k]` 顺序切片。
  (历史:views 消融第一版只取到同一朝向)
- 覆盖:至少包含 1 个边界样本(轨迹端点、视场边缘、极冠附近)。
- 退化路径:至少 1 个零路径 / 室外 / 被遮挡的样本。
  历史上炸在这里:室外点零路径 → drjit 空轴 reduce。
- 通道:多通道实验的 smoke 必须含全部通道,不得只跑 1 个。

### smoke 不能回答的问题(必须标注,不得外推)
- 尾部行为。主指标含 P90 或 RMSE 时,smoke 只验通路,不判优劣。
- 规模相关的数值稳定性(样本数 × 接收机数的交互)。
- 显存与吞吐。

### 通过判据(事先写出,不得事后补)
- 期望的形状、数值范围、运行时间量级,跑之前写下来。
- 结果落在范围内 = 通过;落在范围外 = 报告,不得调范围。

### 必须有的两个对照
- 平凡输入的解析解(单条路径 → 解码角度必须等于该路径的角度)。
- 已知失败样本(教程 RGB 编码 → 方位中位数应 ≈27°)。
  两者都对上,才说明通路是对的。
